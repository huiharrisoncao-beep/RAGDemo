# GraphRAG 知识分享

> 主题：从传统 RAG 到 GraphRAG —— 多跳推理是怎么做到的
>
> 配套 Demo：`Docs/`（双语语料）、`RAG/`（传统 RAG）、`GraphRAG/`（手写轻量 GraphRAG）

---

## 目录

1. [什么是 RAG](#1-什么是-rag)
2. [传统 RAG 的实现原理](#2-传统-rag-的实现原理)
3. [传统 RAG 的结构性局限](#3-传统-rag-的结构性局限)
4. [GraphRAG 的实现原理](#4-graphrag-的实现原理)
5. [GraphRAG 的优势与能解决的问题](#5-graphrag-的优势与能解决的问题)
6. [实战对比案例（贯穿 Demo）](#6-实战对比案例贯穿-demo)
7. [GraphRAG 如何做到：逐跳拆解](#7-graphrag-如何做到逐跳拆解)
8. [适用边界与取舍](#8-适用边界与取舍)

---

## 1. 什么是 RAG

RAG（Retrieval-Augmented Generation，检索增强生成）= **检索** + **生成**。

大模型的知识受限于训练数据、且容易「幻觉」。RAG 的思路是：回答问题前，先从外部语料里**检索**出相关资料，把资料塞进提示词，再让大模型基于资料**生成**答案。

```
问题 ──▶ [检索器] ──相关资料──▶ [大模型] ──▶ 答案
                ▲
            外部语料库
```

关键就在于：**检索器能不能把「回答这个问题真正需要的资料」都找回来。** 这也是传统 RAG 与 GraphRAG 的分水岭。

---

## 2. 传统 RAG 的实现原理

传统 RAG 用**向量相似度**做检索：

```
① 切块     把语料切成若干文本块 (chunk)
② 向量化   每个块用 embedding 模型编码成一个向量
③ 建库     所有向量存入向量数据库 (如 Faiss)
④ 检索     问题也编码成向量，在库中找「最相似」的 top-k 块
⑤ 生成     把 top-k 块拼成上下文，交给大模型生成答案
```

```
        ┌─────────── 离线建库 ───────────┐
语料 ─▶ 切块 ─▶ embedding ─▶ [Faiss 向量库]
                                    │
        ┌─────────── 在线问答 ───────┼──────────┐
问题 ─▶ embedding ─▶ 相似度检索 top-k ┘         │
                          │                     ▼
                       top-k 块 ──▶ 拼接上下文 ──▶ 大模型 ──▶ 答案
```

**核心特征：检索是「扁平的相似度匹配」**——每个块和问题各自算一个向量，比谁更接近。块与块之间没有任何关联结构。

> 对应代码：[`RAG/rag_qa.py`](./RAG/rag_qa.py)

---

## 3. 传统 RAG 的结构性局限

考虑一个**多跳问题**：

> 「云枢智能的 CEO 毕业于哪所大学？那所大学在哪个城市？」

回答它需要串联 3 个事实，而这 3 个事实**分散在不同文档**里：

```
云枢智能 ──的CEO是──▶ 李明        (leadership.md)
李明   ──毕业于──▶ 未名理工大学    (people.md)
未名理工大学 ──位于──▶ 江城        (universities.md)
```

传统 RAG 的问题在于：**问题向量只会召回和「问题整体」最相似的块。**

- 「云枢智能 CEO」相关的块（leadership.md）相似度高 → 召回 ✓
- 「李明毕业于未名理工」这个块**根本不含「云枢智能」**，和问题不相似 → **漏召回 ✗**
- 「未名理工位于江城」这个块**既不含「云枢智能」也不含「李明」** → **更漏 ✗**

这是 Demo 里传统 RAG 的**真实输出**：

```
[检索] top-3 召回片段：
  1. (score=0.513, 来源=leadership.md) 李明现任云枢智能的 CEO…
  2. (score=0.469, 来源=companies.md) 云枢智能是星环科技的全资子公司…
  3. (score=0.422, 来源=universities.md) 北山大学地处云港…   ← 无关块

[生成] 答案：
根据现有资料无法确定。上下文仅提到李明是云枢智能的CEO，
但未说明李明的毕业院校。
```

> ⚠️ 注意：这**不是调参问题**。第二跳、第三跳的线索与问题**天然不相似**，把 top-k 调大也只是引入更多噪声。这是「扁平相似度检索」的**结构性缺陷**。

```
        问题: 云枢智能 CEO 母校 城市?
                    │ 相似度检索
                    ▼
     ┌──────────────────────────────────┐
     │  ✓ 李明是云枢智能CEO   (相似)      │  只拿到第 1 跳
     │  ✗ 李明毕业于未名理工  (不相似,漏) │  ← 断了
     │  ✗ 未名理工位于江城    (不相似,漏) │  ← 断了
     └──────────────────────────────────┘
                    ▼
              链条断裂 → 答不出 / 幻觉
```

---

## 4. GraphRAG 的实现原理

GraphRAG 把「扁平的块集合」换成「**实体-关系图**」：

```
① 抽取   用 LLM 从每篇文档抽取三元组 (主体, 关系, 客体)
② 建图   三元组连成有向图：节点=实体，边=关系（记录来源文档）
③ 遍历   从问题中的实体出发，沿关系边「多跳遍历」，收集整条链
④ 生成   把遍历得到的路径作为上下文，交给大模型生成答案
```

```
        ┌─────────── 离线建图 ───────────┐
语料 ─▶ LLM抽取三元组 ─▶ [知识图谱 (networkx)]
                                 │
        ┌─────────── 在线问答 ────┼───────────────┐
问题 ─▶ 定位起点实体 ─▶ 图上多跳遍历 ┘              │
                          │                        ▼
                     路径三元组 ──▶ 拼接上下文 ──▶ 大模型 ──▶ 答案
```

**核心区别**：检索不再是「比相似度」，而是「**在图上按关系走路径**」。第二跳、第三跳的事实即使和原问题字面不相似，也能通过**边**被精确地找到。

> 对应代码：[`GraphRAG/graphrag_qa.py`](./GraphRAG/graphrag_qa.py)

本 Demo 为「教学版」，三步中间结果全部打印，便于现场观察它是**怎么走通那条链**的。

---

## 5. GraphRAG 的优势与能解决的问题

| 优势 | 能解决什么问题 | 传统 RAG 为何做不到 |
|---|---|---|
| **多跳推理** | 「A 的 B 的 C 是什么」这类需要串联多个事实的问题 | 后续跳与原问题不相似，被漏召回 |
| **跨文档串联** | 线索散落在多篇文档、需要拼起来 | 相似度检索按块独立打分，不理解块间关联 |
| **聚合查询** | 「所有子公司的 CEO 里，谁满足某条件」需要遍历+过滤 | 无「遍历一类邻居」的机制，只能盲猜 top-k |
| **可解释性** | 需要给出「答案是怎么推出来的」 | 只能给出召回块，说不清推理链 |
| **抗幻觉** | 减少凭空编造 | 上下文残缺时模型倾向于「补全」→ 幻觉 |

其中**多跳推理**和**聚合查询**是 GraphRAG 相对传统 RAG 的**质变**能力——不是「效果更好一点」，而是「一个能做、一个根本做不到」。

---

## 6. 实战对比案例（贯穿 Demo）

Demo 用**同一份语料、同一批问题**跑两个程序，可用 [`compare.py`](./compare.py) 并排对比。

**语料被刻意设计**：每条多跳链的相邻两跳事实放在不同文档，且每篇文档不含下一跳关键词（详见 [`Docs/cn/`](./Docs/cn)）。这样传统 RAG 会**可复现地**失败。

### Q1（线性多跳）：云枢智能的 CEO 毕业于哪所大学？那所大学在哪个城市？

| | 传统 RAG | GraphRAG |
|---|---|---|
| 检索方式 | 相似度 top-k | 图上多跳遍历 |
| 拿到的信息 | 只有「李明是CEO」 | 完整链：云枢智能→李明→未名理工→江城 |
| **答案** | ❌ 无法确定 | ✅ 未名理工大学，江城 |

### Q2（聚合多跳）：星环科技旗下所有子公司的 CEO 里，谁的母校在江城？

| | 传统 RAG | GraphRAG |
|---|---|---|
| 需要的操作 | 遍历 3 家子公司 + 各查母校 + 按城市过滤 | 图上逐层展开 + 过滤 |
| 结果 | ❌ 无法确定 | ✅ 李明（未名理工在江城） |

---

## 7. GraphRAG 如何做到：逐跳拆解

这是理解 GraphRAG 的**关键**。以 Q1 为例，看 Demo 的真实输出：

**第一步——抽取**（LLM 把每篇文档变成三元组）：

```
云枢智能 --HAS_CEO--> 李明            [leadership.md]
李明 --GRADUATED_FROM--> 未名理工大学  [people.md]
未名理工大学 --LOCATED_IN--> 江城      [universities.md]
星环科技 --HAS_SUBSIDIARY--> 云枢智能  [companies.md]
... 共 13 条
```

**第二步——建图**（14 个节点，13 条边）：

```
星环科技 ──HAS_SUBSIDIARY──▶ 云枢智能 ──HAS_CEO──▶ 李明
                                                    │
                                             GRADUATED_FROM
                                                    ▼
                                              未名理工大学 ──LOCATED_IN──▶ 江城
```

**第三步——多跳遍历**（从问题里的「云枢智能」出发，沿边走）：

```
[GraphRAG] ③ 从起点实体 ['云枢智能'] 开始多跳遍历：
    第1跳: 云枢智能 --HAS_CEO--> 李明            [来源: leadership.md]
    第2跳: 李明 --GRADUATED_FROM--> 未名理工大学  [来源: people.md]
    第3跳: 未名理工大学 --LOCATED_IN--> 江城      [来源: universities.md]

[生成] 答案：
云枢智能的CEO是李明，李明毕业于未名理工大学，未名理工大学位于江城。
```

**对比 Q2 的聚合遍历**——从「星环科技」出发，图会自动展开成一棵树：

```
    第1跳: 星环科技 --HAS_SUBSIDIARY--> 云枢智能 / 恒星数据 / 光年网络
    第2跳: 云枢智能→李明   恒星数据→王芳   光年网络→陈伟
    第3跳: 李明→未名理工   王芳→明德大学   陈伟→北山大学
    第4跳: 未名理工→江城   明德大学→松岛   北山大学→云港
    ⇒ 过滤「城市=江城」 ⇒ 李明
```

> **本质**：传统 RAG 漏掉的第 2、3 跳，在 GraphRAG 里是**图上明确的边**。遍历沿边走，不依赖「和原问题相不相似」，所以链条不会断。这就是它做到多跳的根本原因。

---

## 8. 适用边界与取舍

GraphRAG 不是银弹，也有代价：

| 维度 | 传统 RAG | GraphRAG |
|---|---|---|
| 建库成本 | 低（只需 embedding） | 高（需 LLM 抽取三元组） |
| 擅长问题 | 单跳、事实查找、语义检索 | 多跳、聚合、关系推理 |
| 依赖 | embedding 质量 | 抽取质量 + 关系设计 |
| 可解释性 | 弱 | 强（有显式路径） |

**实践建议**：两者常**结合使用**——向量检索做粗召回，图结构做关系推理。本 Demo 为突出对比而分开实现。

---

## 附：如何运行 Demo

见 [`README.md`](./README.md)。一句话：配置 `.env` 里的 chat API（embedding 默认用本地模型，离线可用），然后：

```bash
python RAG/rag_qa.py        # 传统 RAG
python GraphRAG/graphrag_qa.py   # GraphRAG
python compare.py           # 并排对比
```
